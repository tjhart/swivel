dependencies {
    compile group: 'vanderbilt.util', name: 'vanderbilt-utilities', version: '1.11-SNAPSHOT'
    compile group: 'org.apache.httpcomponents', name: 'httpclient', version: '4.2.3'
    compile group: 'org.codehaus.jettison', name: 'jettison', version: '1.1'
}

uploadArchives {
    repositories {
        mavenLocal()
        mavenDeployer {
            repository(url: 'http://repo.mc.vanderbilt.edu/nexus/content/repositories/releases') {
                authentication(userName: 'deployment', password: 'mvn123')
            }
            snapshotRepository(url: 'http://repo.mc.vanderbilt.edu/nexus/content/repositories/snapshots/') {
                authentication(userName: 'deployment', password: 'mvn123')
            }
        }
    }
}